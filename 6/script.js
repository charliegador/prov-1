//skapa en funktion isSameUser som accepterar två objekt som argument. Funktionen returnerar true ifall objektens property email är identiska, annars false
function isSameUser({objkt1, objkt2}){
    if (fds){
        
    }
}